/*open Oni_Core;
open Oni_Core_Test;
open Oni_Extensions;
open Oni_ExtensionManagement;
*/

open TestFramework;

describe("Installer", ({test, _}) => {
  test("simple install test", ({expect, _}) => {
    expect.equal(0, 1);
  });
});
