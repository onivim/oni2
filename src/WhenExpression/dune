(ocamllex
  (modules When_lexer))

(menhir
  (modules When_parser))

(library
    (name WhenExpression)
    (public_name Oni2.whenExpression)
    (modules_without_implementation types)
    (libraries  Oni2.core))
