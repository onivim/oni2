// [toggle(lineComment, lines)] returns a new array, of the size of [lines],
// with a comment character applied or removed depending on the input [lines] array.
let toggle: (~lineComment: string, array(string)) => array(string);
